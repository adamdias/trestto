import styled from 'styled-components';

export const Container = styled.div`
  position: relative;
  display: block;
  width: 100%;
  padding: 1.5rem 2rem;

  font-size: 1.5rem;
  font-family: inherit;
  border: none;

  border-radius: 0;
`;
